Taken from http://sourceforge.net/p/pkgbuild/bugs/41/

--- pkgbuild-1.3.105/pkgbuild.pl.in.orig	Thu Apr 18 04:35:25 2013
+++ pkgbuild-1.3.105/pkgbuild.pl.in	Sun Sep 29 00:43:46 2013
@@ -2615,10 +2615,8 @@
     my $tag = shift;
     
     if (defined $tag) {
-	if ($tag =~ /(devel|doc|l10n)/) {
-	    # FIXME: disable automatic tagging for now
-	    # will become facets in the future
-	    $tag = "";
+	if ($tag =~ /(devel|doc)/) {
+	    $tag = " facet.$tag=true";
 	} else {
 	    $tag = "";
 	}
